"use strict"; // Treat all JS code as newer version

// alert(3 + 3)
// As we are using Node.js and it works in the browser
console.log(3
    +
    3);  // Code readability reduced
console.log("Radhika");

// Datatypes
let name = "Hello" // Strings
let age = 28       // Number 
let isStudent = true // Boolean
let bigintValue = 0n // BigInt

// Null = standalone value
// Undefined value 
// Symbol = unique
// Object

console.log(name, age, isStudent)
console.log(typeof age)
